name: gloader

on:
  push:
    paths:
      - "gloader/**"
      - ".github/workflows/gloader.yml"
  pull_request:
    paths:
      - "gloader/**"
      - ".github/workflows/gloader.yml"

jobs:
  build:
    runs-on: windows-latest
    defaults:
      run:
        working-directory: gloader

    steps:
      - uses: actions/checkout@v4

      - name: Set up .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - name: Restore
        run: dotnet restore gloader.sln

      - name: Build
        run: dotnet build gloader.sln -c Release --no-restore

      - name: Publish
        shell: pwsh
        run: ./build.ps1

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: gloader
          path: gloader/dist/gloader/
